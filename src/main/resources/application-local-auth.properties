# Local Authentication Profile
# Activate with: --spring.profiles.active=local-auth

ldapbrowser.auth.mode=local

# Users are stored in ${ldapbrowser.settings.dir}/users.json
# On first run an admin account is created with a random password
# printed to the console.
